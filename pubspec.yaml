name: tempoloco
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.1
  lottie: ^1.2.2

dev_dependencies: null

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/
